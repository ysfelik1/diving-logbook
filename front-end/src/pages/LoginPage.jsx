import React from "react";
import LoginForm from "../components/LoginForm"; // Make sure the path is correct based on your structure

function LoginPage() {
  return <LoginForm />;
}

export default LoginPage;
